<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SevenZipManager.SfxWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="7-Zip self-extracting archive">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Extract to:" Margin="0,0,0,8"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="OutputPathBox"/>
            <Button Grid.Column="1" Content="..." Width="36" Margin="8,0,0,0" Click="OnBrowseClick"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" x:Name="ExtractButton" Content="Extract" MinWidth="120" Click="OnExtractClick"/>
            <Button Grid.Column="2" Content="Cancel" MinWidth="120" Margin="10,0,0,0" Click="OnCancelClick"/>
        </Grid>
    </Grid>
</Window>
